we have the availabity of all the access modifer but accessibility is only for protected and public .
when we have two user and we want to give access two one of them and availabilty to both then we use protected.
==============================================================
when we want to get the public services(fun)  of parent class to child class then we use public inheritance .
===============================================================
